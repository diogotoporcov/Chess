<UserControl x:Class="Chess.Desktop.Views.Screens.ModeSelectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:components="clr-namespace:Chess.Desktop.Views.Components">

    <Grid>

        <Border Margin="48"
                Style="{StaticResource ScreenContentStyle}">

            <StackPanel HorizontalAlignment="Stretch"
                        VerticalAlignment="Top">

                <TextBlock Text="Choose a Mode"
                           Foreground="{StaticResource TextPrimaryBrush}"
                           FontSize="28"
                           FontWeight="SemiBold" />

                <ItemsControl ItemsSource="{Binding Modes}"
                              Margin="0,28,0,0">

                    <ItemsControl.ItemsPanel>

                        <ItemsPanelTemplate>
                            <WrapPanel />
                        </ItemsPanelTemplate>

                    </ItemsControl.ItemsPanel>

                    <ItemsControl.ItemTemplate>

                        <DataTemplate>
                            <components:ModeCardView />
                        </DataTemplate>

                    </ItemsControl.ItemTemplate>

                </ItemsControl>

            </StackPanel>

        </Border>

    </Grid>

</UserControl>
